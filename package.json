{
  "name": "json-server-base",
  "version": "1.0.0",
  "description": "json-server-base",
  "main": "src/server.js",
  "author": "ntdat",
  "license": "MIT",
  "scripts": {
    "start": "npm run generate-data && node src/server.js",
    "dev": "nodemon src/server.js",
    "generate-data": "node src/generate-data",
    "fwrite": "prettier --config .prettierrc --write src",
    "lint": "eslint -c .eslintrc --ext .js,.json src --fix"
  },
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.17.0",
    "query-string": "^7.0.1"
  },
  "devDependencies": {
    "eslint": "^8.0.1",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "nodemon": "^2.0.14",
    "prettier": "^2.4.1"
  }
}
